// class for any object that primarily interacts with or affects a player (weapons, powerups, etc)

import java.awt.*;
import java.awt.image.BufferedImage;

public class PlayerObject extends GameObject {
    int x, y, height, width;
    BufferedImage objectImage;
    public  void drawImage(Graphics g) {

    }
    //add some stuff for weapons and speed categories when that's figured out
}
